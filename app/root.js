import React from 'react-native';

import Login from './views/practice/login';

const {
  Component
} = React;

class Root extends Component {
  render() {
    return (
      <Login />
    );
  }
}

export default Root;
